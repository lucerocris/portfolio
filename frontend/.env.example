# Absolute public URL of the site. Used for canonical links, Open Graph tags
# and the sitemap. Must be set in production.
PUBLIC_SITE_URL=https://your-domain.com

# Base URL of the Payload backend the frontend fetches from.
PUBLIC_CMS_URL=http://localhost:3000
